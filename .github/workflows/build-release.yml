name: Build Windows Release

on:
  push:

permissions:
  contents: write

jobs:
  build-release:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller cryptography plyer

      - name: Build executable with PyInstaller
        run: |
          pyinstaller --noconfirm --clean --onefile dev_webserver.py --name debserv --icon Icon.ico

      - name: Prepare release package
        shell: pwsh
        run: |
          $packageDir = "release-package"
          New-Item $packageDir -ItemType Directory -Force | Out-Null
          Copy-Item -Path "dist/portalwatcher.exe" -Destination $packageDir -Force
          $items = Get-ChildItem $packageDir
          Compress-Archive -Path $items.FullName -DestinationPath "portalwatcher.zip" -Force

      - name: Publish release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: build-${{ github.run_number }}
          name: Automated build ${{ github.run_number }}
          files: portalwatcher.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
